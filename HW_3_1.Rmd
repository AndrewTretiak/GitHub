---
title: "HW_3_1"
author: "Andrey_Tretiak"
date: '28 сентября 2016 г '
output: html_document
---
#Путешественники-транжиры?
##Пункт 1
------
```{r AmericanExpress}
fitr<-lm(Costs ~ Miles, data=American_Express)
summary(fitr)
```

 В этом случае зависимой переменной будет являться переменная Costs, а объясняющей - Miles.
На основе полученных результатах оценки регрессии, выпишем уравнение регрессии:
Costs=274.84969+1.255*Miles
 Смысл данного уравнения состоит в следующем: каждая дополнительная миля путешествия обходится клиентам Americanexpress Company в 1.255 у.е., плюс ко всему этому дополнительных 274.85 у.е. Даже в том случае, если длина путешествия будет равна 0, то стоимость путешествия выйдет в 274.85 у.е.

##Пункт 2
------
```{r AmericanExpress}
confint(fitr)
```

 C помощью данной функции были построены доверительные интервалы по каждой из переменных. Так как коэффициент регрессии лежит внутри полученного доверительного интервала, то можно сделать вывод о том,что он не значим.
 
##Пункт 3
------
```{r AmericanExpress}
new.miles<-data.frame(cost=c(4000,5000))
predict(fitr, data=new.miles)
predict(fitr, data=new.miles, interval=”confidence”)
predict(fitr, data=new.miles, interval=”prediction”)

```

.....

##Пункт 4
------
```{r AmericanExpress}
American_Express$Costs
fitted(fitr)
residuals(fitr)
```
 Наблюдаемые предсказанные значения значительно откланяются от тех данных, которые указаны в таблице. Некотроые из них отклоняются как на 500, так и на 6. Остатки - это разность между предсказанными и реальными значениями.

##Пункт 5
------
```{r AmericanExpress}

```

##Пункт 6
------
```{r AmericanExpress}

```

##Пункт 7
------
```{r AmericanExpress}

```

##Пункт 8
------
```{r AmericanExpress}

```